digraph G {
	rankdir=BT;
	node [shape=polygon];
	edge [group];
	
	{ rank=same; datadriven; solver; combigrid; }

	pde -> base;
	pde -> solver;
	datadriven -> base;
	finance -> pde;
	finance -> base;
	solver -> base;
	parallel -> base;
	parallel -> pde;
	parallel -> finance;
	parallel -> datadriven [constraint=false];
	combigrid -> base;
}
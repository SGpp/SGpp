#!/usr/bin/make -f

SCONS=scons 

%:
	dh $@

override_dh_auto_build:
	$(SCONS) -j8 PREFIX=$(DEB_DESTDIR)/usr COMPILE_BOOST_TESTS=0

override_dh_auto_clean:
	dh_auto_clean
	$(SCONS)  -c

override_dh_auto_install:
	$(SCONS) PREFIX=$(DEB_DESTDIR)/usr install

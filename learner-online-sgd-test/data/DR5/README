6D DR5 Dataset for scalability tests
MGS clean. deredded ugriz+eClass
obtained with the following SQL Query:
SELECT S.modelMag_u as modelMag_u, S.modelMag_g as modelMag_g, 
S.modelMag_r as modelMag_r, S.modelMag_i as modelMag_i, S.modelMag_z as modelMag_z,
O.eClass as eClass, S.z as redshift from SpecPhotoAll S, SpecObjAll O, Galaxy G
WHERE
     S.specObjID = O.specObjID
     AND S.objID = G.objID
     AND S.zConf >= 0.95


    and (G.flags & ( dbo.fPhotoFlags('BLENDED') + dbo.fPhotoFlags('NODEBLEND') + 
    dbo.fPhotoFlags('CHILD'))) != dbo.fPhotoFlags('BLENDED') 
    
    and (G.flags & (dbo.fPhotoFlags('EDGE') + dbo.fPhotoFlags('SATURATED'))) = 0 

     AND (S.status & dbo.fPhotoStatus('OK_RUN')) != 0
     AND (S.status & dbo.fPhotoStatus('GOOD')) != 0
     AND (S.status & dbo.fPhotoStatus('PRIMARY')) != 0

    AND S.zWarning = 0
    AND S.modelMag_u <= 22.0
    AND S.modelMag_g <= 22.2
    AND S.modelMag_r <= 22.2
    AND S.modelMag_i <= 21.3
    AND S.modelMag_z <= 20.5
AND (
(S.primTarget & dbo.fPrimTarget("TARGET_GALAXY")) != 0
OR (S.primTarget & dbo.fPrimTarget("TARGET_GALAXY_BIG")) != 0
OR (S.primTarget & dbo.fPrimTarget("TARGET_GALAXY_BRIGHT_CORE")) != 0

)
